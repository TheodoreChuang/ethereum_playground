const { expect } = require("chai");

describe("LiquidityPool", () => {
  describe.skip("deposit()", () => {
    it("Should allow LP to deposit and mint governance tokens (single deposit)", () => {});
    it("Should allow LP to deposit and mint governance tokens (multiple deposits)", () => {});
  });
  describe.skip("withdraw()", () => {
    it("Should allow LP to withdraw and mint governance tokens", () => {});
    it("Should revert if LP tried to withdraw more than their deposits", () => {});
  });
});
